numbers = [5, 8, 3, 1, 9, 2]
min_value = numbers[0]

for i in range(1, len(numbers)):
    if numbers[i] < min_value:
        min_value = numbers[i]

print(f"Minimalna wartość w tablicy to: {min_value}")
