n = int(input("Podaj liczbę n: "))
negative_count = 0
numbers = []

for i in range(n):
    number = int(input(f"Podaj {i+1}. liczbę: "))
    numbers.append(number)
    if number < 0:
        negative_count += 1

print(f"Ilość liczb ujemnych w ciągu wynosi: {negative_count}")
