#Zadanie 1
def szuk(element):
    lista = [1, 2, 3, 4, 5, 6, 7, 8, 9]
    if element in lista:
        print('element jest na liście')
    else:
        print('element nie  jest na liście')
print(szuk(1))
